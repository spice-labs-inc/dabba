.terraform/
*.tfstate
*.tfstate.*
*.tfvars
.terraform.lock.hcl
crash.log
kubeconfig
/target

# re-include docs in case a global excludes file ignores markdown
!*.md

# per-env working dirs (tofu state + kubeconfig)
.dabba/
.DS_Store
